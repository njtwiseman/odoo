# -*- coding: utf-8 -*-

from odoo import models, fields, api

class OreillyVehicle(models.Model):
    _name = 'oreilly.vehicle' 
#    name = fields.Char('Name', index=True, required=True, translate=True)
